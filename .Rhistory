# Load required packages
library(rgbif)
library(tidyverse)
library(terra)
library(sp)
library(sdmpredictors)
library(CoordinateCleaner)
setwd("C:/Users/hl51981/OneDrive - University of Georgia/Leavitt_Herbert/PFFW/Manuscripts/Global Change/Revision_repository")
options(
gbif_user = Sys.getenv("GBIF_USER"),
gbif_email = Sys.getenv("GBIF_EMAIL"),
gbif_pwd = Sys.getenv("GBIF_PWD")
)
# Load presence data
presence_df <- read.csv("presence_pivot_merged_sp.csv")
# Calculate mean occurrence for each species
presence_df$mean_occurrence <- rowMeans(presence_df[, c("X2006", "X2016", "X2022_2023")], na.rm = TRUE)
# Filter to species-level taxa (space in name) or defined groups
species_level <- presence_df[grepl(" ", presence_df$Taxon), ]
# Unique taxa (including pooled groups)
target_taxa <- unique(species_level$Taxon) %>% rbind("Avicennia germinans", "Spartina alterniflora")
# Define manual taxon groupings for gbif search. Will be recompiled afterwards in STI calculations later on.
group_taxa <- list(
"Minuca spp." = c("Minuca longisignalis"),
"Palaemon spp." = c("Palaemon pugio", "Palaemon vulgaris"),
"Panopeus spp." = c("Panopeus obesus", "Panopeus simpsoni")
)
# Loop through taxa
for (taxon in target_taxa) {
message("Processing ", taxon)
# Determine which species to query
if (taxon %in% names(group_taxa)) {
species_vec <- group_taxa[[taxon]]
} else {
species_vec <- taxon
}
for (sp in species_vec) {
key <- name_backbone(name = sp)$usageKey
# Submit the download request
download_key <- occ_download(
pred("taxonKey", key),
pred("hasCoordinate", TRUE),
pred("hasGeospatialIssue", FALSE),
pred("basisOfRecord", "HUMAN_OBSERVATION"),
pred_gte("year", 2005),
format = "SIMPLE_CSV"
)
occ_download_wait(download_key)
# Save raw zip
dl <- occ_download_get(download_key, path = "gbif_downloads", overwrite = TRUE)
clean_data <- occ_download_import(dl)
# Skip species if no records are returned
if (nrow(clean_data) == 0) {
warning("No data returned for ", sp)
next
}
# Clean the data
clean_data <- clean_data %>%
setNames(tolower(names(.))) %>%
filter(occurrencestatus == "PRESENT") %>%
filter(!basisofrecord %in% c("FOSSIL_SPECIMEN", "LIVING_SPECIMEN")) %>%
filter(year >= 1900) %>%
filter(coordinateprecision < 0.01 | is.na(coordinateprecision)) %>%
filter(coordinateuncertaintyinmeters < 10000 | is.na(coordinateuncertaintyinmeters)) %>%
filter(!coordinateuncertaintyinmeters %in% c(301, 3036, 999, 9999)) %>%
filter(!(decimallatitude == 0 | decimallongitude == 0)) %>%
cc_cen(buffer = 2000) %>%
cc_cap(buffer = 2000) %>%
cc_inst(buffer = 2000) %>%
distinct(decimalLongitude, decimalLatitude, specieskey, datasetkey, .keep_all = TRUE)
# Save cleaned data
csv_path <- paste0("gbif_downloads/clean_csvs/", gsub(" ", "_", sp), "_clean.csv")
write.csv(clean_data, csv_path, row.names = FALSE)
# Log citation or DOI
meta <- occ_download_meta(download_key)
if (!is.null(meta$citation) && nzchar(meta$citation)) {
cat(paste0(sp, ": ", meta$citation, "\n\n"), file = "gbif_citations.txt", append = TRUE)
} else if (!is.null(meta$doi) && nzchar(meta$doi)) {
cat(paste0(sp, ": https://doi.org/", meta$doi, "\n\n"), file = "gbif_citations.txt", append = TRUE)
} else {
warning("No DOI or citation found for ", sp)
cat(paste0(sp, ": [No DOI found]\n\n"), file = "gbif_citations.txt", append = TRUE)
}
}
}
print(faithful)
#lets take a look
plot(faithful) #axes aren't right, explanatory variable should be on the x axis.
plot(x=faithful$waiting, y=faithful$eruptions)
#This model takes the form of y= mx + b
#we can extract the coeffsicients the intercept and m with the coefficients command
coeffs<- coefficients(eruption.lm)
#When runing linear models (or any analysis) R treats the model as an object.
#So it is always best to name the model
#
#liniar regression = lm() response ~ predictor
eruption.lm <- lm(eruptions~waiting, data=faithful)
eruption.lm #gives you the coefficients of the linear model(intercepts and slope
#This model takes the form of y= mx + b
#we can extract the coeffsicients the intercept and m with the coefficients command
coeffs<- coefficients(eruption.lm)
coeffs
#We can now fit any point or a series of points to the model
#Input the waiting time
waiting<- 80
#now input the model and set the duration (y-intercept)
duration<- coeffs[1] + coeffs[2]*waiting
#but we dont know if this data is normal yet
hist(faithful$eruptions)
plot(eruption.lm)#this plots the residuals of your data. There should be no pattern at all on this data. if there is a pattern then a linear model isnt appropriate to your data
#The predict command will apply the function in the object eruption.lm and use the data we say
predict(eruption.lm, times)
#Now ask R how long the eruption will last
duration
#Now we can creat or upload a data frame and predict any value
times<- data.frame(waiting=c(80,100,120,140))
#Now ask R how long the eruption will last
duration
#Now we can creat or upload a data frame and predict any value
times<- data.frame(waiting=c(80,100,120,140))
#The predict command will apply the function in the object eruption.lm and use the data we say
predict(eruption.lm, times)
#Now ask R how long the eruption will last
duration
#Now we can creat or upload a data frame and predict any value
times<- data.frame(waiting=c(80,100,120,140))
#The predict command will apply the function in the object eruption.lm and use the data we say
predict(eruption.lm, times)
#We can even name the function and plot our new data
eruptions.predict<-predict(eruption.lm, times)
#Now Plot it
plot(eruptions.predict, main="Predicted Eruption Duration", xlab= "Waiting Times", ylab="Duration")
points(y=faithful$eruptions, x=faithful$waiting)
#Determining the Coefficient of Determination and summary stats
eruption.lm = lm(eruptions ~ waiting, data=faithful)
#We use summary to give the base stats
summary(eruption.lm)
summary(eruption.lm)$r.squared
#We use summary to give the base stats
summary(eruption.lm)
#but we dont know if this data is normal yet
hist(faithful$eruptions)
plot(eruption.lm)#this plots the residuals of your data. There should be no pattern at all on this data. if there is a pattern then a linear model isnt appropriate to your data
plot(eruption.lm)#this plots the residuals of your data. There should be no pattern at all on this data. if there is a pattern then a linear model isnt appropriate to your data
#Now ask R how long the eruption will last
duration
hist(faithful$eruptions)
plot(eruption.lm)#this plots the residuals of your data. There should be no pattern at all on this data. if there is a pattern then a linear model isnt appropriate to your data
View(faithful)
View(faithful)
View(faithful)
#We can now fit any point or a series of points to the model
#Input the waiting time
waiting<- 60 # ow input the model and set the duration (y-intercept)sd++5
duration<- coeffs[1] + coeffs[2]*waiting
#Now ask R how long the eruption will last
duration
observed_duration <- faithful %>% filter(waiting = 60)
observed_duration <- faithful %>% filter(waiting == 60)
View(observed_duration)
observed_duration <- faithful %>% filter(waiting == 60) %>%
mutate(residuals = eruptions - predicted_duration)
#We can now fit any point or a series of points to the model
#Input the waiting time
waiting<- 60 # ow input the model and set the duration (y-intercept)sd++5
predicted_duration<- coeffs[1] + coeffs[2]*waiting
#Now ask R how long the eruption will last
predicted_duration
observed_duration <- faithful %>% filter(waiting == 60) %>%
mutate(residuals = eruptions - predicted_duration)
print(observed_duration$residuals)
plot(eruption.lm)#this plots the residuals of your data. There should be no pattern at all on this data. if there is a pattern then a linear model isnt appropriate to your data
#Now we can creat or upload a data frame and predict any value
times<- data.frame(waiting=c(80,100,120,140))
#The predict command will apply the function in the object eruption.lm and use the data we say
predict(eruption.lm, times)
#We can even name the function and plot our new data
eruptions.predict<-predict(eruption.lm, times)
#Now Plot it
plot(eruptions.predict, main="Predicted Eruption Duration", xlab= "Waiting Times", ylab="Duration")
points(y=faithful$eruptions, x=faithful$waiting)
#Determining the Coefficient of Determination and summary stats
eruption.lm = lm(eruptions ~ waiting, data=faithful)
#We use summary to give the base stats
summary(eruption.lm)
#You can avoid the mess by calling just the inforation you want
names(summary(eruption.lm)#As you have propabaly already learned this summary tables are awkward to look at
)
summary(eruption.lm)$r.squared
#Determining confidence intervals
predict(eruption.lm, times, interval="confidence")
#Determine the prediction intervals
predict(eruption.lm, newdata, interval="predict")
#Plotting the residual error
eruption.lm = lm(eruptions ~ waiting, data=faithful)
eruption.res = resid(eruption.lm)
#Plot the residuals
plot(faithful$waiting, eruption.res,
ylab="Residuals", xlab="Waiting Time",
main="Old Faithful Eruptions")
abline(0,0)
#QQ plots
qqnorm(eruption.res,
ylab="Standardized Residuals",
xlab="Normal Scores",
main="Old Faithful Eruptions")
qqline(eruption.res)
print(stackloss)
#lets take a look
plot(faithful) #axes aren't right, explanatory variable should be on the x axis.
# Sometimes the best way to ensure your model is working is to plot in alongside the data. Try that here
# Step 1: Scatterplot of raw data
plot(faithful$waiting, faithful$eruptions,
xlab = "Waiting Time (min)",
ylab = "Eruption Duration (min)",
main = "Eruption Duration vs Waiting Time")
# Step 2: Add regression line from the model
abline(eruption.lm, col = "red", lwd = 2)
print(stackloss)
print(stackloss)
print(stackloss)
#look at the data
plot(stackloss)
#Now we create the linear model using the multiple parameters
stackloss.lm = lm(log(stack.loss) ~
Air.Flow + Water.Temp + Acid.Conc.,
data=stackloss)
plot(stackloss.lm)
#Now we create the linear model using the multiple parameters
stackloss.lm = lm(stack.loss ~
Air.Flow + Water.Temp + Acid.Conc.,
data=stackloss)
plot(stackloss.lm)
Water.Temp=20,
#Now we create the linear model using the multiple parameters
stackloss.lm = lm(log(stack.loss) ~
Air.Flow + Water.Temp + Acid.Conc.,
data=stackloss)
plot(stackloss.lm)
hist(log(stackloss$stack.loss))
#Like before we can create a predictor variable to predict any value
newdata = data.frame(Air.Flow=72,
Water.Temp=20,
Acid.Conc.=85)
#Get the summary of the model
summary(stackloss.lm)
#You can avoid the mess by calling just the inforation you want
names(summary(eruption.lm)#As you have propabaly already learned this summary tables are awkward to look at
)
